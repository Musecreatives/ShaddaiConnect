NEXT_PUBLIC_API_URL=http://localhost:3000/api
# No Flutterwave key needed here: the API initializes checkout server-side (Standard/hosted
# flow) and this app just redirects to the URL it returns. Was NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY
# — removed 2026-09-02 (switch to Flutterwave), and it was never actually read anywhere anyway.

# Support/contact channels — placeholders until you provide real values. WhatsApp number in
# international format with no + or spaces (e.g. 2348012345678) for the wa.me deep link.
NEXT_PUBLIC_WHATSAPP_NUMBER=
NEXT_PUBLIC_SUPPORT_EMAIL=
