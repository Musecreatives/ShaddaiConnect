// Shared with PlansService.findPublic(), which excludes this plan from the public buy-site list
// by this same name — single source of truth so the two stay in sync.
export const TRIAL_PLAN_NAME = 'Free Trial';
